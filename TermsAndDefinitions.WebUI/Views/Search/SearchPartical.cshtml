

@if(ViewData["query"] == null)
    {
        //Html.RenderAction("FundArias");
    }
    else
    {
        Html.RenderAction("Terms", new { queryString = ViewData["query"].ToString()});
        Html.RenderAction("Projects", new { queryString = ViewData["query"].ToString()});
    }

