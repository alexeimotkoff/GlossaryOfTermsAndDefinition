@model IEnumerable<TermsAndDefinitions.WebUI.ViewModels.PreviewTermViewModel>

@foreach (var item in Model)
{
    <div class="text-cut-box">
        <div>
            <p>
                @Ajax.RouteLink(item.TermName, "RouteToTerm", new {name = item.TermName}, new AjaxOptions { UpdateTargetId = "Main" }, new { @class="term"}) — @item.Definition
            </p>
        </div>
    </div>
}

